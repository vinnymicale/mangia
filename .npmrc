# better-sqlite3 ships a prebuilt binary that is Node 22+ only -- on Node 20 it
# segfaults inside the Database constructor rather than throwing, so a version
# mismatch is unrecoverable at runtime. Fail at install time instead.
engine-strict=true
